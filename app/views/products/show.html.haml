%p#notice= notice
%h1= @product.name
%p= number_to_currency @product.price
%p= cl_image_tag(@product.image_url, height: "300")

= link_to 'Edit', edit_product_path(@product)
\|
= link_to 'Back', products_path
