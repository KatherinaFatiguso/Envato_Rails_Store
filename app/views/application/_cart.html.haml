-# if a cart exists and it's not empty, then render the text
-if @cart
  %p.navbar-text.navbar-right
    -if @cart.empty?
      Your cart is empty.
    -else
      =link_to "You have #{pluralize @cart.count, "item"}.", cart_path
